# ============================================================
# Requirements — Hinglish Grounded Medical RAG
#
# Every package here is imported by code that exists in this repo.
# Previously this file pinned peft / trl / bitsandbytes / open_clip_torch /
# wandb / pydicom / ollama for QLoRA, DPO and BioMedCLIP work that was never
# implemented, while OMITTING `groq`, which is imported at module load in three
# files — so a clean install could not run the system at all.
# ============================================================

# ---------- Retrieval: encoder + index ----------
sentence-transformers>=3.0.0     # LaBSE, multilingual-e5, MuRIL
torch>=2.1.0
transformers>=4.40.0
faiss-cpu>=1.8.0

# ---------- Retrieval: lexical baselines ----------
rank_bm25>=0.2.2                 # BM25 baseline (src/analysis/h4_baselines.py)
scikit-learn>=1.5.0              # TF-IDF baseline, cosine similarity

# ---------- Generation ----------
groq>=0.11.0                     # the generator API — REQUIRED at import time
python-dotenv>=1.0.0             # loads GROQ_API_KEY from .env

# ---------- Data ----------
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0                  # passage index is stored as .parquet
datasets>=2.19.0                 # HuggingFace dataset download
huggingface_hub>=0.23.0
multiversity                     # MultiCaRe corpus access (src/data/download_multicare.py)

# ---------- Statistics ----------
scipy>=1.13.0                    # Wilcoxon, McNemar, Mann-Whitney, Spearman, bootstrap

# ---------- Figures ----------
matplotlib>=3.9.0                # all publication figures (300 DPI + vector PDF)

# ---------- Demo ----------
streamlit>=1.35.0                # app.py

# ---------- Config & utilities ----------
PyYAML>=6.0.0
tqdm>=4.66.0
requests>=2.32.0
Pillow>=10.3.0

# ---------- Tests ----------
pytest>=8.0.0
